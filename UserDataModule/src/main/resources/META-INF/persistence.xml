<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             version="2.2"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <persistence-unit name="UserPersistenceUnit" transaction-type="JTA">
        <!-- Nome del DataSource configurato in WildFly -->
        <jta-data-source>java:/MySqlDS</jta-data-source>

        <!-- Impostazioni Hibernate -->
        <properties>
            <!-- Crea automaticamente le tabelle (puoi usare anche 'update' o 'validate') -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Dialect per MySQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>

            <!-- Mostra query nel log (opzionale, utile per debug) -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

</persistence>
